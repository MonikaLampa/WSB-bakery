import { BacklightDirective } from './backlight.directive';

describe('BacklightDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new BacklightDirective();
  //   expect(directive).toBeTruthy();
  // });
});
